# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

find_package(VTK)
INCLUDE(${VTK_USE_FILE})

include(pitukiTaskLib)
ADD_LIBRARY(${PITUKI_TASKLIB_NAME} SHARED 
    ${PITUKI_TASKLIB_SOURCES})
add_dependencies(${PITUKI_TASKLIB_NAME}
    regen-typekit DEPS_CMAKE VTK)

TARGET_LINK_LIBRARIES(${PITUKI_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${PITUKI_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${PITUKI_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${PITUKI_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${PITUKI_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${PITUKI_TASKLIB_HEADERS}
    DESTINATION include/orocos/pituki)

